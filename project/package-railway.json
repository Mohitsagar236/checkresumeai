{
  "name": "checkresumeai-railway",
  "version": "1.0.0",
  "description": "CheckResumeAI Backend for Railway",
  "main": "backend/dist/server.js",
  "type": "module",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js",
    "postinstall": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
