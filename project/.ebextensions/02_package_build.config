container_commands:
  01_npm_install_root:
    command: "npm install"
  02_build_backend:
    command: "cd backend && npm install typescript --save && npm run build"
    env:
      NODE_ENV: production
